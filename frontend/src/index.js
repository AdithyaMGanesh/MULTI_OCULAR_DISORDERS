import React from 'react';
import ReactDOM from 'react-dom/client';
// Import the new Dual-Model App component
import AppDualModel from './AppDualModel';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <AppDualModel />
  </React.StrictMode>
);
